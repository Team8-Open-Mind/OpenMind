import { css } from 'styled-components';

const Layout = () => {
  return (
    <div
      css={css`
        background-color: green;
      `}
    >
      Layout
    </div>
  );
};

export default Layout;
